import CarouselComponent from "./Carousel";
import { HeroWrapper } from "./styles";

const Hero = () => {
  return (
    <HeroWrapper>
      <CarouselComponent />
    </HeroWrapper>
  );
};

export default Hero;
